package application.function;

public class Rotation {
    public Rotation(double rotation){
        double a0=Math.cos(rotation);
        double b0=Math.sin(rotation);
        double a1=-b0;
        double b1=a0;
        
    }
}
